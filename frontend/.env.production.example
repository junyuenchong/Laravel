## Production env (Vite)
#
# If you deploy frontend and backend on the same domain and proxy /api via nginx,
# leave VITE_API_BASE_URL empty so requests go to /api/* on the same origin.
VITE_API_BASE_URL=

