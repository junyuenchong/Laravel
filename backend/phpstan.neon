includes:
    - vendor/nunomaduro/larastan/extension.neon

parameters:
    paths:
        - app
        - database
        - tests

    level: 5

    checkMissingIterableValueType: false

    ignoreErrors:
        # You can add patterns here if there are known safe issues.
        # - '#Some known false-positive#'

